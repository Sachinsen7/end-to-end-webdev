// const superHeros = []

// superHeros.push("spiderman")   // gives the error

// const superHeros: string[] = [];
// const heroPower: number[] = [];

heroPower.push(4);

superHeros.push("spiderman");

type Users = {
  name: string;
  isActive: boolean;
};

// push

const allUser: Users[] = [];

allUser.push({ name: "sachin", isActive: true });

// one more way

const Mlmodels: number[][] = [[255, 255, 255], []];
